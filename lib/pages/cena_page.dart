import 'package:flutter/material.dart';

class CenaPage extends StatelessWidget {
  const CenaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Cena') //para identificar en que pantalla estamos
      ),
    );
  }
}